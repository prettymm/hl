"use strict";

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

$.mlpFnName = function (fn) {
  return fn.name || (fn + '').split(/\s|\(/)[1];
};

$.mlpInit = function (fn, name, set) {
  if (typeof set == "undefined") {
    set = true;
  }
  window.MLP = window.MLP || {};
  window.MLP.apps = window.MLP.apps || {};
  window.MLP.instances = window.MLP.instances || {};
  window.MLP.config = window.MLP.config || {};
  if (fn && set) {
    name = name || $.mlpFnName(fn);
    window.MLP.apps[name] = fn;
  } else {
    return window.MLP.apps[fn];
  }
};

/***
@fn (obj) - the JS object class
@name (str) - the name of the class
@bypass (bool) - whether or not the module can be instantiated multiple times
@elModule (bool) - whether to create an element based module $(...).ModuleName 
or regular JQuery method $.MethodName
@returnContext (bool) - whether to return an object with class context 
and the element which instantiated the module (@elModule must be true)
**/

$.mlpModule = function (fn, name, bypass, elModule, returnContext) {
  var obj;
  if (typeof bypass == "undefined") {
    bypass = false;
  }
  if (typeof elModule == "undefined") {
    elModule = true;
  }
  if (typeof returnContext == "undefined") {
    returnContext = true;
  }
  obj = {};
  $.mlpInit(fn, name);
  name = name || $.mlpFnName(fn);
  obj[name] = function () {
    var args,
        option,
        _el,
        _this = [],
        result;
    option = arguments[0];
    args = 2 <= arguments.length ? Array.prototype.slice.call(arguments, 1) : [];
    _el = $(this).each(function () {
      var $this, data, key;
      $this = $(this);
      key = 'mlp-' + name;
      data = $this.data(key);
      if (!data || bypass) {
        $this.data('name', name);
        $this.data(key, data = new fn(option, this));
      }
      if (typeof option === 'string') {
        return data[option].apply(data, args);
      }
      _this.push(data);
    });
    result = returnContext ? { mlp: _this, el: _el } : null;
    return result;
  };
  if (elModule) {
    $.fn.extend(obj);
  } else {
    obj[name] = function (option) {
      return new fn(option);
    };
    $.extend(obj);
  }
};

var MLPModule = function () {
  _createClass(MLPModule, [{
    key: "defaults",
    value: function defaults() {
      this.defaults = {};
    }
  }]);

  function MLPModule(options, element) {
    _classCallCheck(this, MLPModule);

    this.ops = $.extend({}, this.defaults, options);
    this.sel = this.sel || {};
    this.el = this.el || {};
    this.el.target = $(element);
    this.init();
    return this;
  }

  _createClass(MLPModule, [{
    key: "init",
    value: function init() {
      this.classes = {
        active: 'is-active',
        offscreen: 'l-offscreen',
        ghost: 'l-ghost'
      };

      this.keys = {
        esc: 27,
        down: 40,
        up: 38,
        left: 37,
        right: 39,
        o: 79,
        space: 32,
        tab: 9,
        enter: 13
      };

      this.aria = {
        expanded: 'aria-expanded',
        hidden: 'aria-hidden',
        controls: 'aria-controls',
        selected: 'aria-selected',
        invalid: 'aria-invalid',
        pressed: 'aria-pressed',
        described: 'aria-describedby',
        checked: 'aria-checked',
        label: 'aria-label',
        labelled: 'aria-labelledby',
        popup: 'aria-haspopup'
      };

      this.attr = {
        tabindex: 'tabindex'
      };
    }
  }, {
    key: "stop",
    value: function stop(e) {
      e.preventDefault();
      return e.stopPropagation();
    }
  }, {
    key: "target",
    value: function target(e) {
      return $(e.target);
    }
  }, {
    key: "currentTarget",
    value: function currentTarget(e) {
      return $(e.currentTarget);
    }
  }, {
    key: "isRight",
    value: function isRight(e) {
      return this.keycode(e) === this.keys.right;
    }
  }, {
    key: "isLeft",
    value: function isLeft(e) {
      return this.keycode(e) === this.keys.left;
    }
  }, {
    key: "isUp",
    value: function isUp(e) {
      return this.keycode(e) === this.keys.up;
    }
  }, {
    key: "isDown",
    value: function isDown(e) {
      return this.keycode(e) === this.keys.down;
    }
  }, {
    key: "isEnter",
    value: function isEnter(e) {
      return this.keycode(e) === this.keys.enter;
    }
  }, {
    key: "isSpace",
    value: function isSpace(e) {
      return this.keycode(e) === this.keys.space;
    }
  }, {
    key: "isTab",
    value: function isTab(e) {
      return this.keycode(e) === this.keys.tab;
    }
  }, {
    key: "isBackTab",
    value: function isBackTab(e) {
      return e.shiftKey && this.isTab(e);
    }
  }, {
    key: "isEsc",
    value: function isEsc(e) {
      return this.keycode(e) === this.keys.esc;
    }
  }, {
    key: "isEnterOrSpace",
    value: function isEnterOrSpace(e) {
      return this.isEnter(e) || this.isSpace(e);
    }
  }, {
    key: "keycode",
    value: function keycode(e) {
      return e.which;
    }
  }, {
    key: "isIE",
    value: function isIE(version) {
      if (typeof version == "undefined") {
        version = 9;
      }
      return bowser.msie && bowser.version === version;
    }
  }]);

  return MLPModule;
}();

$.mlpInit(MLPModule, 'MLPModule');
'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _get = function get(object, property, receiver) { if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { return get(parent, property, receiver); } } else if ("value" in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*
  country state city
 */

var CountryDirectory = function (_MLP$apps$MLPModule) {
  _inherits(CountryDirectory, _MLP$apps$MLPModule);

  function CountryDirectory() {
    _classCallCheck(this, CountryDirectory);

    return _possibleConstructorReturn(this, (CountryDirectory.__proto__ || Object.getPrototypeOf(CountryDirectory)).apply(this, arguments));
  }

  _createClass(CountryDirectory, [{
    key: 'init',
    value: function init() {
      _get(CountryDirectory.prototype.__proto__ || Object.getPrototypeOf(CountryDirectory.prototype), 'init', this).call(this);
      this.sel.state = this.el.target.data('state-selector');
      this.sel.city = this.el.target.data('city-selector');
      this.events();
    }
  }, {
    key: 'events',
    value: function events() {
      var _this2 = this;

      $(this.sel.state).parent().hide();
      $(this.sel.city).parent().parent().hide();
      $(this.el.target).on("change", function () {

        _this2.populateStateData($(_this2.el.target).val());
      });
      $(this.sel.state).on("change", function (e) {

        _this2.populateCityData(e.target.value);
      });
    }
  }, {
    key: 'populateStateData',
    value: function populateStateData(countryCode) {

      var hasOption = false;
      $(this.sel.state).val('');
      $(this.sel.state).parent().show();
      $(this.sel.state).attr("disabled", false);
      $(this.sel.state).children().each(function (index, item) {
        if ($(item).data('country') == countryCode) {

          $(item).show();
          hasOption = true;
        } else {
          $(item).hide();
        }
      });

      if (!hasOption) {
        $(this.sel.state).parent().hide();
        $(this.sel.city).parent().parent().hide();
      } else {
        $(this.sel.state).parent().show();
      }
    }
  }, {
    key: 'populateCityData',
    value: function populateCityData(cityCode) {
      var hasOption = false;
      $(this.sel.city).val('');
      $(this.sel.city).parent().show();
      $(this.sel.state).attr("disabled", false);
      $(this.sel.city).children().each(function (index, item) {
        if ($(item).data('city') == cityCode) {

          $(item).show();
          hasOption = true;
        } else {
          $(item).hide();
        }
      });
      if (!hasOption) {
        $(this.sel.city).parent().hide();
      } else {
        $(this.sel.city).parent().parent().show();
      }
    }
  }]);

  return CountryDirectory;
}(MLP.apps.MLPModule);

$.mlpModule(CountryDirectory, 'CountryDirectory');
'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _get = function get(object, property, receiver) { if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { return get(parent, property, receiver); } } else if ("value" in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*
  For i18n
 */

var SetPostion = function (_MLP$apps$MLPModule) {
  _inherits(SetPostion, _MLP$apps$MLPModule);

  function SetPostion() {
    _classCallCheck(this, SetPostion);

    return _possibleConstructorReturn(this, (SetPostion.__proto__ || Object.getPrototypeOf(SetPostion)).apply(this, arguments));
  }

  _createClass(SetPostion, [{
    key: 'init',
    value: function init() {
      _get(SetPostion.prototype.__proto__ || Object.getPrototypeOf(SetPostion.prototype), 'init', this).call(this);
      this.event();
    }
  }, {
    key: 'event',
    value: function event() {

      $(document).on('click', '.js-down-arrow', function (e) {
        e.preventDefault();
        var scrollTop = $(window).width > 767 ? $(e.target).offset().top + 85 : $(e.target).offset().top + 65;
        $('html, body').animate({
          scrollTop: scrollTop
        });
      });
    }
  }]);

  return SetPostion;
}(MLP.apps.MLPModule);

$.mlpModule(SetPostion, 'SetPostion');
'use strict';

/*
  Every plugin/module initialization goes here.
 */
$(document).ready(function () {

  console.log('APAC MY18 CAMPAIN');

  //i18n
  $('[data-js-i18next]').I18next();

  //set bike value
  $('[data-js-setBikeValue]').SetBikeValue();

  //down arrow link
  $(".js-down-arrow").SetPostion();

  //datepicker
  $('[data-js-datePicker]').DatePicker();

  //slick carousel
  $('.js-select-bike').slick({
    centerMode: false,
    centerPadding: '0px',
    slidesToShow: 2.5,
    slidesToScroll: 1,
    infinite: false,
    arrows: false
  });

  //form validator
  $('[data-js-form-validator]').Validator();

  //three-level country state city
  $('[data-js-country-directory]').CountryDirectory();
});
'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _get = function get(object, property, receiver) { if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { return get(parent, property, receiver); } } else if ("value" in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*
  For i18n
 */

var Fullpage = function (_MLP$apps$MLPModule) {
  _inherits(Fullpage, _MLP$apps$MLPModule);

  function Fullpage() {
    _classCallCheck(this, Fullpage);

    return _possibleConstructorReturn(this, (Fullpage.__proto__ || Object.getPrototypeOf(Fullpage)).apply(this, arguments));
  }

  _createClass(Fullpage, [{
    key: 'defaults',
    value: function defaults() {
      this.defaults = {
        property: 'foo'
      };
    }
  }, {
    key: 'init',
    value: function init() {
      this.el = {
        thankYou: $('.js-thankyou-fullpage')
      };
      _get(Fullpage.prototype.__proto__ || Object.getPrototypeOf(Fullpage.prototype), 'init', this).call(this);
      this.events();
      $(window).resize(function () {
        this.fullPage();
      });
    }
  }, {
    key: 'events',
    value: function events() {
      this.fullPage();
    }
  }, {
    key: 'fullPage',
    value: function fullPage() {
      var windowHeight = $(window).height();
      var footerHeight = $('.footer').outerHeight();
      console.log(windowHeight);
      this.el.thankYou.height(windowHeight - footerHeight);
    }
  }]);

  return Fullpage;
}(MLP.apps.MLPModule);

$.mlpModule(Fullpage, 'Fullpage');
'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _get = function get(object, property, receiver) { if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { return get(parent, property, receiver); } } else if ("value" in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*
  For i18n
 */

var I18next = function (_MLP$apps$MLPModule) {
  _inherits(I18next, _MLP$apps$MLPModule);

  function I18next() {
    _classCallCheck(this, I18next);

    return _possibleConstructorReturn(this, (I18next.__proto__ || Object.getPrototypeOf(I18next)).apply(this, arguments));
  }

  _createClass(I18next, [{
    key: 'init',
    value: function init() {
      _get(I18next.prototype.__proto__ || Object.getPrototypeOf(I18next.prototype), 'init', this).call(this);
      var _lang = this.el.target.data('lang');
      var langArr = ['en', 'zh', 'zh-tw', 'jp', 'ko', 'th', 'vi', 'id'];

<<<<<<< HEAD
      if (!this.contains(_lang, langArr)) {
        _lang = 'en';
      }

      if (_lang === "en") {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "en",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      } else if (_lang === "zh") {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "zh",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      } else if (_lang === "zh-tw") {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "zh-tw",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      } else if (_lang === "jp") {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "jp",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      } else if (_lang === "ko") {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "ko",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      } else if (_lang === "th") {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "th",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      } else if (_lang === "vi") {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "vi",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      } else {
        i18next.use(window.i18nextXHRBackend).init({
          debug: false,
          load: 'all',
          lng: _lang,
          resGetPath: '../Content/locale/{{lng}}.json',
          fallbackLng: "id",
          backend: {
            loadPath: '../Content/locale/{{lng}}.json',
            addPath: '../Content/locale/add/{{lng}}',
            allowMultiLoading: false
          }
        }, function (err, t) {
          jqueryI18next.init(i18next, $);
          return $('body').localize();
        });
      }
=======
      i18next.use(window.i18nextXHRBackend).init({
        debug: false,
        load: 'all',
        lng: _lang,
        resGetPath: '../Content/locale/{{lng}}.json',
        fallbackLng: "en",
        backend: {
          loadPath: '../Content/locale/{{lng}}.json',
          addPath: '../Content/locale/add/{{lng}}',
          allowMultiLoading: false
        }
      }, function (err, t) {
        jqueryI18next.init(i18next, $);
        return $('body').localize();
      });
>>>>>>> master
    }
  }, {
    key: 'contains',
    value: function contains(lang, langArr) {
      for (var i in langArr) {
        if (this[i] == lang) return true;
      }
      return false;
    }
  }]);

  return I18next;
}(MLP.apps.MLPModule);

$.mlpModule(I18next, 'I18next');
"use strict";

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _get = function get(object, property, receiver) { if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { return get(parent, property, receiver); } } else if ("value" in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*
  For i18n
 */

var SetBikeValue = function (_MLP$apps$MLPModule) {
  _inherits(SetBikeValue, _MLP$apps$MLPModule);

  function SetBikeValue() {
    _classCallCheck(this, SetBikeValue);

    return _possibleConstructorReturn(this, (SetBikeValue.__proto__ || Object.getPrototypeOf(SetBikeValue)).apply(this, arguments));
  }

  _createClass(SetBikeValue, [{
    key: "init",
    value: function init() {
      _get(SetBikeValue.prototype.__proto__ || Object.getPrototypeOf(SetBikeValue.prototype), "init", this).call(this);
      this.className = {
        active: 'active'
      };
      this.event();
    }
  }, {
    key: "event",
    value: function event() {
      var _this2 = this;

      $(".js-select-selector").off("click").on("click", function (evt) {
        var $target = $(evt.target).parent();

        $(".js-select-selector").removeClass(_this2.className.active);
        $target.addClass(_this2.className.active);
        var dataValue = $target.data("value");
        if (dataValue) {
          $(".js-select-box").val(dataValue);
        }
      });
    }
  }]);

  return SetBikeValue;
}(MLP.apps.MLPModule);

$.mlpModule(SetBikeValue, 'SetBikeValue');
'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _get = function get(object, property, receiver) { if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { return get(parent, property, receiver); } } else if ("value" in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*
  For i18n
 */

var DatePicker = function (_MLP$apps$MLPModule) {
  _inherits(DatePicker, _MLP$apps$MLPModule);

  function DatePicker() {
    _classCallCheck(this, DatePicker);

    return _possibleConstructorReturn(this, (DatePicker.__proto__ || Object.getPrototypeOf(DatePicker)).apply(this, arguments));
  }

  _createClass(DatePicker, [{
    key: 'init',
    value: function init() {
      _get(DatePicker.prototype.__proto__ || Object.getPrototypeOf(DatePicker.prototype), 'init', this).call(this);
      this.event();
    }
  }, {
    key: 'event',
    value: function event() {
      var _this2 = this;

      var startDate = new Date(),
          endDate = new Date();
      startDate.setDate(startDate.getDate() + 2);
      endDate = new Date(endDate.getFullYear() + 1, endDate.getMonth(), 0);
      $('#preferred_date').datetimepicker({
        minView: 'month',
        startDate: startDate,
        endDate: endDate,
        format: 'dd MM yyyy',
        autoclose: true,
        todayBtn: true,
        pickerPosition: 'bottom-left'
      }).on('hide', function () {
        return $(_this2).siblings('label.form__label').addClass('form__label--filled');
      });
    }
  }]);

  return DatePicker;
}(MLP.apps.MLPModule);

$.mlpModule(DatePicker, 'DatePicker');
"use strict";

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _get = function get(object, property, receiver) { if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { return get(parent, property, receiver); } } else if ("value" in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } };

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

/*
  For i18n
 */

var Validator = function (_MLP$apps$MLPModule) {
  _inherits(Validator, _MLP$apps$MLPModule);

  function Validator() {
    _classCallCheck(this, Validator);

    return _possibleConstructorReturn(this, (Validator.__proto__ || Object.getPrototypeOf(Validator)).apply(this, arguments));
  }

  _createClass(Validator, [{
    key: "init",
    value: function init() {
      _get(Validator.prototype.__proto__ || Object.getPrototypeOf(Validator.prototype), "init", this).call(this);
      this.sel.errorMsg = ".input-error-message";
      this.sel.callback = this.el.target.attr('data-callback');
      this.events();
    }
  }, {
    key: "events",
    value: function events() {
      var _this3 = this;

      this.el.target.on("submit", function (evt) {
        if (!_this3.validate()) {
          return false;
        }
        window[_this3.sel.callback].apply();
        return true;
      });
    }
  }, {
    key: "validate",
    value: function validate() {
      var _this4 = this;

      var _isPassed, _this;
      this.hideErrorMsg();
      _isPassed = true;
      _this = this;
      $(this.el.target.context).find(" :input").each(function (index, item) {
        var _rulesSplit;
        if ($(item).is(":visible") || $(item).data("js-visible")) {
          var _rules = $(item).data('rule');
          if (_rules) {
            _rulesSplit = _rules.split("|");
            $.each(_rulesSplit, function (index, rule) {
              if (!_this4.isRulePassed($(item), rule)) {
                $(item).siblings('.input-error-message').removeClass('hide');
                $(item).parent().siblings('.input-error-message').removeClass('hide');
                _isPassed = false;
              }
            });
          }
        }
      });
      return _isPassed;
    }
  }, {
    key: "isRulePassed",
    value: function isRulePassed(_this, rule) {
      var _isValid = true,
          number;
      var splitRule = rule.split(':');
      if (splitRule[0] == 'max' || splitRule[0] == 'min') {
        rule = splitRule[0];
        number = splitRule[1];
      }

      switch (rule) {
        case 'required':
          if (_this.attr('type') == 'checkbox') {
            if (!_this.is(':checked')) {
              _isValid = false;
            }
          } else {
            if ($.trim(_this.val()).length === 0) {
              _isValid = false;
            }
          }
          break;

        case 'max':
          if ($.trim(_this.val()).length > number) {
            _isValid = false;
          }
          break;
        case 'max':
          if ($.trim(_this.val()).length > number) {
            _isValid = false;
          }
          break;
        case 'min':
          if ($.trim(_this.val()).length < number) {
            _isValid = false;
          }
          break;
        case 'noZZOnBeginning':
          if (_this.val().toLowerCase().substr(0, 2) === 'zz') {
            _isValid = false;
          }
          break;
        case 'firstThreeNoRepeat':
          var pattern = /([a-zA-Z0-3])\1{2,}/;
          if (pattern.test(_this.val())) {
            _isValid = false;
          }
          break;
        case 'alpha':
          var pattern = /[0-9~`!@#\$\%\^&*()_+,\.\/\;\'[\]\\<>\?\:\"\{\}\|～！@＃¥％……&＊（）＋——－＝、］［｛｝｜‘；：“／。，《》？]/i;
          if (pattern.test(_this.val())) {
            _isValid = false;
          }
          break;
        case 'email':
          var pattern = /^([a-z\d!#$%&'*+\-\/=?^_`{|}~\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]+(\.[a-z\d!#$%&'*+\-\/=?^_`{|}~\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]+)*|"((([ \t]*\r\n)?[ \t]+)?([\x01-\x08\x0b\x0c\x0e-\x1f\x7f\x21\x23-\x5b\x5d-\x7e\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]|\\[\x01-\x09\x0b\x0c\x0d-\x7f\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]))*(([ \t]*\r\n)?[ \t]+)?")@(([a-z\d\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]|[a-z\d\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF][a-z\d\-._~\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]*[a-z\d\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])\.)+([a-z\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]|[a-z\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF][a-z\d\-._~\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF]*[a-z\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])\.?$/i;
          if (!pattern.test(_this.val())) {
            _isValid = false;
          }
          break;
        case 'mobile':
          var pattern = /^(?:(?:\(?(?:00|\+)([1-4]\d\d|[1-9]\d?)\)?)?[\-\.\ \\\/]?)?((?:\(?\d{1,}\)?[\-\.\ \\\/]?){0,})(?:[\-\.\ \\\/]?(?:#|ext\.?|extension|x)[\-\.\ \\\/]?(\d+))?$/i;
          if (!pattern.test(_this.val())) {
            _isValid = false;
          }
      }

      return _isValid;
    }
  }, {
    key: "hideErrorMsg",
    value: function hideErrorMsg() {
      $(this.sel.errorMsg).addClass('hide');
    }
  }]);

  return Validator;
}(MLP.apps.MLPModule);

$.mlpModule(Validator, 'Validator');